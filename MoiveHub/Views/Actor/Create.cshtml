@model Actor
@{
    ViewData["Title"] = "Add New Actor";
}
<div class="row-text">
   <div class="col-md-8 offset-2">
       <p>
           <h1>Add new Actor</h1>
       </p>
       <div class="row">
            <div class="col-md-8">
                <form asp-action="Create">
                    <div asp-validation-summary="ModelOnly" class="text-danger">
                    </div>
                    <div class="form-group">
                      
                        <img id="ActorUrlpre" class="border-info rounded-circle" style="max-width: 150px" />

                    </div>
                    <div class="form-group">
                        <lable asp-for="ActorUrl" class="control-label">ActorUrl</lable>
                        <input asp-for="ActorUrl" class="form-control" />
                        <span asp-validation-for="ActorUrl" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <lable asp-for="ActorName" class="control-label">ActorName</lable>
                        <input asp-for="ActorName" class="form-control" />
                        <span asp-validation-for="ActorName" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <lable asp-for="Bio" class="control-label">Bio</lable>
                        <input asp-for="Bio" class="form-control" />
                        <span asp-validation-for="Bio" class="text-danger"></span>
                    </div>
                    <div class="form-group">

                        <input type="submit" value="Create" class="btn btn-outline-success float-right" />
                        <a class="btn btn-outline-secondary " asp-action="Index">Show All</a>

                    </div>
                </form>

            </div>
       </div>
       
   </div>

</div>

@section scripts{
    <script>

        $(document).ready(function () {
            var output = document.getElementById('ActorUrlpre');
            output.src = $("#ActorUrl").val();
        })


        $("#ActorUrl").on("change", function () {
            var output = document.getElementById('ActorUrlpre');
            output.src = $(this).val();
        })
    </script>
}
<style>
        .form-group{
            margin:10px 10px
        }
</style>
               